# ── Web ──────────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9      # 파일 업로드 (multipart/form-data)
jinja2==3.1.4

# ── DB ───────────────────────────────────────────────────────────────────────
sqlalchemy==2.0.35
asyncpg==0.31.0              # PostgreSQL 비동기 드라이버
alembic==1.13.3
greenlet==3.1.1              # SQLAlchemy async 실행 필요

# ── Config ───────────────────────────────────────────────────────────────────
pydantic-settings==2.5.2

# ── 오디오 캡처 (브라우저 → WebSocket) ─────────────────────────────────────
# 캡처는 브라우저(getDisplayMedia)에서 수행, 서버는 WebSocket으로 수신만 함
# pyaudiowpatch 불필요 (Windows WASAPI 방식 제거)

# ── STT (Phase 1 — 벤치마크 후 하나로 확정 예정) ────────────────────────────
faster-whisper==1.1.1        # STT 확정 (2026-05-11, 1.1.4 미존재→1.1.1)
requests>=2.32.0             # faster-whisper 내부 의존성 (huggingface-hub 1.x가 httpx로 전환해 누락됨)
openai==1.56.0               # LLM 클라이언트 — Ollama OpenAI-compatible 엔드포인트 사용
# whisperx==3.1.5            # 미사용 — faster-whisper로 확정

# ── Dev ──────────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2                # FastAPI TestClient 비동기 버전
