# TODO(phase1): LLM 퀴즈 생성 구현 (LLM 선정 후 확정)


def generate_quizzes(transcript_text: str, num_questions: int = 5) -> list[dict]:
    """전사 텍스트를 받아 CLAUDE.md 스키마에 맞는 퀴즈 목록을 반환한다."""
    # TODO(phase1): LLM 호출 → JSON 파싱 → 스키마 검증
    raise NotImplementedError("LLM 선정 후 구현 예정")
